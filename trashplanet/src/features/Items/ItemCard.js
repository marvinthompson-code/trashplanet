import React from "react"
import { NavLink, useHistory } from "react-router-dom"

const ItemCard = ({ item }) => {
    const history = useHistory();

    return (
        <div>

        </div>
    )
}